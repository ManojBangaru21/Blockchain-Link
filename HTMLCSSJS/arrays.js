// let a = [1,2,3]

// //console.log(a.length)
// a.push(4) // add an element at the end of an array
// a.unshift(0) // add an element at the beginning of an array
// a.pop(2) // removes an item from the end
// a.shift()// removes first element of an array
// //console.log(a)

// let b = [10,`Manoj`,[5,6]]
// //console.log(b)

// let c = a.concat(b) // add two arrays

// //console.log(c)

// let d = [...a,...b] // using spread operator '...' to add 2 arrays
// console.log(d)